source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in onionurigen.gemspec
gemspec

gem 'base32'
gem 'openssl_pkcs8_pure'